diff -Naur netkit-telnet-0.16/configure netkit-telnet-0.16-p/configure
--- netkit-telnet-0.16/configure	Sun Dec 12 19:36:00 1999
+++ netkit-telnet-0.16-p/configure	Wed Jul 12 16:46:35 2000
@@ -60,7 +60,8 @@
 
 BINDIR="$EXECPREFIX/bin"
 SBINDIR="$EXECPREFIX/sbin"
-MANDIR="$PREFIX/man"
+DATADIR="$PREFIX/share"
+MANDIR="$DATADIR/man"
 
 echo "Directories: $BINDIR $SBINDIR $MANDIR "
 
@@ -171,7 +172,6 @@
          $CC -O2 __conftest.c -o __conftest
        ) >/dev/null 2>&1; then
          echo 'yes'
-         CFLAGS="$CFLAGS -O2"
     else
          echo 'no'
          echo -n "Checking if $CC accepts -O... "
@@ -547,6 +547,7 @@
 
     echo "BINDIR=$BINDIR"
     echo "SBINDIR=$SBINDIR"
+    echo "DATADIR=$DATADIR"
     echo "MANDIR=$MANDIR"
     echo "BINMODE=$BINMODE"
     echo "DAEMONMODE=$DAEMONMODE"
@@ -557,7 +558,7 @@
     echo "CC=$CC"
     echo "CXX=$CXX"
     if [ x$CC_WARNINGS != x ]; then
-        CFLAGS="$CFLAGS $WARNINGS"
+        CFLAGS="$CFLAGS $WARNINGS -DINET6"
     fi
 
     if [ x$CXX_WARNINGS != x ]; then
diff -Naur netkit-telnet-0.16/telnet/Makefile netkit-telnet-0.16-p/telnet/Makefile
--- netkit-telnet-0.16/telnet/Makefile	Sun Aug  1 07:06:37 1999
+++ netkit-telnet-0.16-p/telnet/Makefile	Wed Jul 12 16:46:50 2000
@@ -7,7 +7,7 @@
 
 # -DAUTHENTICATE
 CXXFLAGS += -DUSE_TERMIO -DKLUDGELINEMODE
-LIBS += $(LIBTERMCAP)
+LIBS += $(LIBTERMCAP) -lresolv
 
 SRCS = commands.cc main.cc network.cc ring.cc sys_bsd.cc telnet.cc \
 	terminal.cc tn3270.cc utilities.cc genget.cc environ.cc netlink.cc
